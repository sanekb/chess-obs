const S = 1e3;
const M = 60 * S;
const H = 60 * M;

export const WATCH_MODE_INTERVAL = 60 * S;
export const WATCH_MODE_AUTO_OFF = 2 * H / M;
export const API_THROTTLE_TTL = 5 * S;

export const TOOLTIP_DELAY = 2 * S;
export const RECONNECT_DELAYS = [5 * S, 10 * S, 30 * S, 60 * S];

export const GM_SCORE = 3100;

export const PRIZE_PER_WIN = 5e3;
export const PRIZE_FOR_GMs = 10e3;
export const PRIZE_FOR_TOP = 10e3;

export const APP_NAME = "chess-obs";
export const TAG = "__INIT_DATA__";
